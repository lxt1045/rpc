
TODO:
1. trunk 实现把多个网络连接合并成一个网路连接的供能，不过好像BUG还没解完。
2. 新建一个 trunk_kcp 目录，实现 trunk 目录相同的功能。
3. trunk_kcp 使用 KCP 传递数据，其方案如下:
  - 发送端先发送给 kcp ，然后将kcp 输出的数据发送给一个chan;
  - 每个网络链接处理循环都监听chan，当收到一个数据包之后，同步发送到他管理的那个网络上。
  - 网络链接接收端处理循环接收链接发送过来数据后，发送到另一个chan上； 另一个goroutine 收到chan的数据包后，传递给接收端kcp，然后kcp的输出端作为整体输出。
